<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PhotoGallery.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PhotoGallery"
    Shell.FlyoutBehavior="Flyout"
    Title="PhotoGallery">


    <Shell.FlyoutHeader>
        <StackLayout Padding="20" Spacing="10">
            <Button Text="Dodaj kontakt" Clicked="OnAddClicked" BackgroundColor="White" TextColor="Black" FontAttributes="Bold"/>
            <Button Text="Edytuj kontakt" Clicked="OnEditClicked" BackgroundColor="White" TextColor="Black" FontAttributes="Bold"/>
            <Button Text="Usuń kontakt" Clicked="OnDeleteClicked" BackgroundColor="White" TextColor="Black" FontAttributes="Bold"/>
        </StackLayout>
    </Shell.FlyoutHeader>
    <FlyoutItem>
        <ShellContent Route="MainPage" ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>
</Shell>
